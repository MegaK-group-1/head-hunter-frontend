import React, { Component } from "react";
import styled from "styled-components";

export class UserForm extends Component<any, any> {
        constructor(props: any) {
                super(props);
        }
        render() {
                return (
                    < >
                            <h1>Formularz aktualizacji danych</h1>
                    </>
                );
        }
}